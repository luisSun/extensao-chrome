// Este é um exemplo simples de como você pode interagir com o conteúdo da página
// Pode ser personalizado conforme suas necessidades específicas

// Este código é executado em todas as páginas que correspondem ao "matches" definido no manifest.json

// Exemplo: Alterar o texto de todos os links na página para "Link alterado"
/*document.querySelectorAll('a').forEach(function(element) {
    element.textContent = 'Link alterado';
});

// Exemplo: Modificar o estilo de todos os parágrafos na página
document.querySelectorAll('p').forEach(function(element) {
    element.style.fontFamily = 'Arial, sans-serif';
    element.style.color = '#333';
});

// Você pode adicionar mais lógica aqui para interagir com o conteúdo da página conforme necessário
*/